// Package cookbook is the executable posting cookbook: it drives one example from
// every product theme through the real ledger and asserts the books balance, so the
// mappings in docs/posting-cookbook.md are proven, not just described. It has no
// runtime code of its own — the value is in cookbook_test.go.
package cookbook
